export type product = {
    id: number,
    name: string,
    image: string,
    price: number,
    category: string,
    stock?: number,
    amount?: number,
    createdAt?: Date
    updatedAt?: Date
}
